---
/**
 * Static by design. This was briefly a React island with per-item
 * write-ups behind toggles; both the toggles and the write-ups made
 * the hero work harder than an introduction should. Five lines, no JS.
 */
const doing = [
  'Build products',
  'Teach AI to engineers and non-coders',
  'Read research papers',
  'Build visualisation and teaching tools',
  'Help startups',
];
---

<div class="flex w-full min-w-0 flex-col">
  <p class="nameplate text-primary">Bengaluru</p>

  <h1 class="mt-3 text-4xl sm:text-5xl">I'm Sidharth. I:</h1>

  <ul class="mt-7 space-y-3 text-xl sm:text-2xl">
    {
      doing.map((item) => (
        <li class="flex gap-3.5">
          <span class="mt-[0.62em] h-px w-4 shrink-0 bg-primary" aria-hidden="true" />
          <span>{item}</span>
        </li>
      ))
    }
  </ul>

  <p class="mt-8 max-w-[52ch] text-muted-foreground">
    16+ years working with startups. Currently at Outskill; before that Headout, Instamojo and
    more. I also run a{' '}
    <a
      href="https://youtube.com/@kingsidharth"
      rel="noopener"
      class="text-primary hover:underline">YouTube channel</a
    >.
  </p>
</div>
